# 云端定时任务配置

云端任务每天北京时间 08:00 与 12:00 运行，连接私有资格档案仓库和本公开网站仓库。私有仓库仅用于资格门禁，任何私人值均不得复制到公开仓库。

任务每轮先完整读取公开仓库 `AGENTS.md`、`data/source-plan.json`、`data/source-registry.json`、`data/screening-policy.json` 与 `data/filter-recipes.json`，再执行同步。常规定时任务使用 **GPT-5.6 Terra / 中等推理**；Sol 高推理不参与每天的扫描，只用于每周或人工触发的少量疑难复核。核心要求如下：

> 高召回发现京考、国考、北京定向选调与优培、北京及中央事业单位，以及北京优先的央国企具体岗位。搜索不得只限医学；要覆盖生物医学工程、工学、理工类、相关专业与不限专业。公考下载完整官方职位表后按全部资格字段组合筛选；央国企先用官网结构化筛选，再遍历筛选结果的全部分页。第三方渠道仅用于发现，公开内容必须回到官方原文或官方招聘系统核验。
>
> 大型招聘入口先使用官网自己的招聘类型、届别、地点、学历、岗位类别、发布时间和关键词筛选，不得直接翻完整分页。安全筛选后不超过 100 个时处理全部结果；仍超过 150 个时才运行多组广义关键词并取并集、按职位代码去重。北京每轮检查，其他地区按周轮换。只有没有可靠站内筛选时才下载官方表格或用代码处理官方分页；公考职位表在本地用字段组合筛选。去重候选约 40 个一批复核，再只打开拟发布和疑难岗位详情。高推理每轮最多处理 20 个真正冲突项，其余 deferred 后续继续。
>
> 只要官方公告或预公告已发布且截止未过，就纳入追踪，即使报名尚未开始。有职位表时拆到具体岗位并标记“即将开放”；暂无职位表时写入审核日志并持续追踪。关键与 active 官方入口失败时共尝试 3 次并按登记顺序切换官方备用入口；仍失败则标记本轮 `completed-partial`，不得把访问失败解释为没有新公告。不能只看 HTTP 200：最终地址或页面正文是 404/error 时记 `semantic-404`；页面能打开但筛选、分页、附件或职位详情没处理完时记 `accessible-incomplete`，不能记成 `temporarily-unavailable`。
>
> 公考与选调必须使用已授权私有档案逐项通过全部硬条件才可发布；任一项未知、不符或需要咨询都不得写入公开 jobs。央国企必须具体到岗位名称或代码并记录单位、地点、届别、学历、专业、职责、发布时间、截止口径和官方投递路径。岗位排序优先北京、资格确定、专业覆盖、质量、待遇、强度和发展，但低排名不等于排除。
>
> 只依据官方可核验事实排除明显低待遇、长期高强度、高危、有害暴露、长期夜班倒班、极高频出差、长期外派或重体力岗位。未公开薪资或强度不算负面事实；不得使用行业或性别刻板印象。
>
> 无论正文是否变化，都追加 `policyVersion: 4`、`screeningStrategyVersion: 2` 的审核日志，逐一记录本轮全部官方来源、状态、尝试次数、检查时间及结论；每轮记录 `screeningMetrics`，至少包含入口报告总量、站内查询次数、站内筛选结果、去重候选、批量复核数、官方逐岗核验数、高推理升级数和额度延后数，没有相应数据时填 0。更新分钟级北京时间、监测项时间和逐岗 `lastSeenAt/lastSeenStatus/statusEvidence`。运行全部验证脚本，全部通过后提交并推送。最后报告新增、变更、关闭、未通过、继续核验、来源失败与是否部分完成。

公开仓库的 GitHub Pages 流程会再次校验来源计划、岗位数据、审核日志与匿名边界后自动发布。
