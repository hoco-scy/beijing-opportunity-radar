# 云端定时任务配置

云端任务每天北京时间 08:00 与 12:00 运行，连接私有资格档案仓库和本公开网站仓库。私有仓库仅用于资格门禁，任何私人值均不得复制到公开仓库。

任务每轮先完整读取公开仓库 `AGENTS.md`、`data/source-plan.json`、`data/source-registry.json` 与 `data/screening-policy.json`，再执行同步。常规定时任务使用 **GPT-5.6 Terra / 中等推理**；Sol 高推理不参与每天的全量扫描，只用于每周或人工触发的少量疑难复核。核心要求如下：

> 高召回发现京考、国考、北京定向选调与优培、北京及中央事业单位，以及北京优先的央国企具体岗位。搜索不得只限医学；要覆盖生物医学工程、工学、理工类、相关专业与不限专业。公考下载并遍历完整官方职位表；央国企遍历官方列表全部分页和职位板块。第三方渠道仅用于发现，公开内容必须回到官方原文或官方招聘系统核验。
>
> 综合入口达到 200 个岗位时，禁止让模型逐岗读取，也禁止把整表粘贴进上下文。下载一次官方完整数据并计算 SHA-256，用代码遍历全部行完成明确硬条件初筛；只把机器通过和疑难项按每批约 40 个交给模型输出结构化 JSON，再对拟发布、疑难和现有正文岗位打开官方详情页。附件未变化时复用上轮机器结果。机器排除项按原因抽样复核；误杀时修改规则并重跑全表。高推理每轮最多处理 20 个真正冲突项，剩余项标记 deferred 后续继续，不得因此减少发现总量。
>
> 只要官方公告或预公告已发布且截止未过，就纳入追踪，即使报名尚未开始。有职位表时拆到具体岗位并标记“即将开放”；暂无职位表时写入审核日志并持续追踪。关键与 active 官方入口失败时共尝试 3 次并切换官方备用入口；仍失败则标记本轮 `completed-partial`，不得把访问失败解释为没有新公告。
>
> 公考与选调必须使用已授权私有档案逐项通过全部硬条件才可发布；任一项未知、不符或需要咨询都不得写入公开 jobs。央国企必须具体到岗位名称或代码并记录单位、地点、届别、学历、专业、职责、发布时间、截止口径和官方投递路径。岗位排序优先北京、资格确定、专业覆盖、质量、待遇、强度和发展，但低排名不等于排除。
>
> 只依据官方可核验事实排除明显低待遇、长期高强度、高危、有害暴露、长期夜班倒班、极高频出差、长期外派或重体力岗位。未公开薪资或强度不算负面事实；不得使用行业或性别刻板印象。
>
> 无论正文是否变化，都追加 `policyVersion: 3` 的审核日志，逐一记录本轮全部官方来源、状态、尝试次数、检查时间及结论；每轮都记录 `screeningMetrics`，至少包含发现总数、机器扫描/排除数、批量复核数、官方逐岗核验数、高推理升级数和额度延后数，没有相应数据集时填 0。更新分钟级北京时间、监测项时间和逐岗 `lastSeenAt/lastSeenStatus/statusEvidence`。运行五个验证脚本，全部通过后提交并推送。最后报告新增、变更、关闭、未通过、继续核验、来源失败与是否部分完成。

公开仓库的 GitHub Pages 流程会再次校验来源计划、岗位数据、审核日志与匿名边界后自动发布。
