# 云端定时任务配置

在 ChatGPT 网页版使用每天北京时间 08:00 和 12:00 运行的独立任务，并连接：

- 私有资料仓库：仅用于资格判断，不向外输出；
- 本公开网站仓库：只写入完成核验且可公开的岗位记录。

可使用以下任务说明：

> 每天两次更新“北京求职雷达”。先阅读公开仓库中的 AGENTS.md。广泛搜索京考、国考、北京定向选调与优培、北京事业单位，以及工作地点在北京的央国企 2027 届具体岗位。第三方渠道只用于发现线索，必须回到政府官方公告、官方职位表、企业官网或企业确认的官方招聘系统核验。公考和选调岗位必须使用已授权的私有资料逐项核对全部硬条件，只有所有条件明确通过才允许写入公开 jobs；任一条件未知、不符或需要咨询都不得发布。央国企岗位必须具体到岗位名称或代码，并准确记录单位、地点、届别、学历、专业、职责、发布时间、截止口径和官方投递路径。不得把个人资料、私人资格值、未核验线索或第三方链接提交到公开仓库。
>
> 无论是否有岗位通过正文门禁，每轮成功运行都必须追加一条 `data/review-log.json` 运行记录，并更新 `data/opportunities.json` 的 `meta.lastVerifiedAt`、审核日志 `meta.lastRunAt` 及监测项 `checkedAt`。时间统一使用北京时间、带 `+08:00` 且精确到分钟的 ISO 时间戳，例如 `2026-08-22T08:03:00+08:00`。审核日志记录官方来源成功/失败数量、发现/审核/通过/未通过/继续核验数量，以及每个已回溯官方来源候选的审核层级、结论、公开安全原因和官方兜底入口。原因不得暴露候选人的具体私人条件；未回溯到官方来源的第三方线索仍只留在私有报告。
>
> 复查现有岗位状态，更新数据、审核日志和核验时间，运行 `node scripts/validate-data.mjs`、`node scripts/validate-review-log.mjs` 与 `node scripts/check-privacy.mjs`。三项全部通过后，即使正文新增/更新/关闭均为 0，也要提交并推送，让公开网站留下本轮运行记录。最后报告新增、变更、关闭、未通过和继续核验的线索。

公开仓库的部署流程会在岗位数据、审核日志、分钟级核验时间和匿名检查全部通过后自动发布 GitHub Pages。
